// Audio/TTS services removed as per user request to resolve sync and performance issues.
export const toggleMute = (muted: boolean) => {};
export const speakLocally = (text: string, onEnd?: () => void) => {};
export const playNarration = async (text: string, isDemo: boolean = false) => {};
